spring:
  cloud:
    nacos:
      discovery:
        service: gateway
        namespace: dev
        server-addr: 192.168.17.14:8848
        register-enabled: true
        heart-beat-timeout: 30
        group: ruan
      config:
        namespace: dev
        encode: UTF-8
        server-addr: 192.168.17.14
        timeout: 3000
        file-extension: yaml
        max-retry: 3
        group: ruan
        refresh-enabled: false
server:
  port: 10010
